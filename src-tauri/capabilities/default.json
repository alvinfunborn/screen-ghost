{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and overlay window",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "core:event:default"
  ]
}
